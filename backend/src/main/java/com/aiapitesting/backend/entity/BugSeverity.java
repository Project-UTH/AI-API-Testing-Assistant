package com.aiapitesting.backend.entity;

/** Mức độ nghiêm trọng của bug (Module 10) - MINOR đứng đầu làm mặc định (@Builder.Default). */
public enum BugSeverity {
    MINOR,
    SERIOUS,
    CRITICAL
}
